import { nextNode } from './nodeHandler.js'
import { setup } from './setup.js'
import { startScreen } from './imageDisplay.js'

const game = setup();

startScreen(game);
//display image and image button
//start screen and start button

// nextNode(game);